function src_weapon_shooting(_damage, _recoil, _recoil_push, _length_to_barrel, _cooldown, _bullet_speed, _ammo_count, _sprite_used){
    //mouse button
    var _mouse_button = mouse_check_button(mb_left);
    
    if (_mouse_button)
    {
        if (_cooldown == 0)
        {
            
        }
    }


}